server:
  port: 8006
spring:
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  application:
    name: lab-service
  datasource:
    url: jdbc:mysql://localhost:3306/brain_health?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: bh_user
    password: bh_pass_2024
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379
      password: redis_pass_2024
logging:
  level:
    com.brainhealth: DEBUG
    root: INFO
brain-health:
  lab-report:
    storage-root: ${java.io.tmpdir}/brain-health/lab-reports
